# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing basic website route navigation
GET   /               controllers.HomeController.index
GET   /login          controllers.HomeController.login
GET   /department          controllers.HomeController.department
GET   /coordinator	  controllers.HomeController.coordinator
GET   /:page          controllers.HomeController.loadPage(page)
POST  /formSubmit     controllers.HomeController.formSubmit

# Map static resources from the /public folder to the /assets URL path
GET   /assets/*file   controllers.Assets.versioned(path="/public", file: Asset)
